<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.haustinsl.haustin">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar"
        tools:targetApi="31">

    <!-- Actividad Principal -->
    <activity
        android:name=".MainActivity"
        android:exported="true"
        android:label="@string/app_name">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>

    <!-- Actividades de Navegación Principal -->
    <activity android:name=".VenderActivity" />
    <activity android:name=".ComprarActivity" />
    <activity android:name=".AlquilarActivity" />
    <activity android:name=".PerfilActivity" />

    <!-- Actividades de Detalle del Perfil -->
    <activity android:name=".Perfil.PerfilDetalleActivity" />
    <activity android:name=".Perfil.PerfilCompradorDetalleActivity" />
    <activity android:name=".Perfil.PerfilArrendadorDetalleActivity" />
    <activity android:name=".Perfil.PerfilArrendatarioDetalleActivity" />
    <activity android:name=".Perfil.PerfilVendedorDetalleActivity" />
    <activity android:name=".Perfil.HaustinPremiumDetalleActivity" />
    <activity android:name=".Perfil.AyudaAsistenciaDetalleActivity" />

    </application>


</manifest>